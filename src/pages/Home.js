import React from "react";

import GameList from "../components/GameList/GameList";

const sectionsConfig = [
    {}
];

const Home = () => {
    return (
        <main className="main">
            <GameList />
        </main>
    )
};

export default Home;